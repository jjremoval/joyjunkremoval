# macOS
.DS_Store

# Local tooling / config
.claude/

# Temporary working files (previews, screenshots)
temp_screenshots/

# Private working folders — local only, never deployed (synced via Google Drive)
# docs/ = all reference docs + reports + review CSVs (markdown/CSV)
# tools/ = scraper program files + working data
docs/
tools/
*.pdf

# Node boilerplate (static site, no build step)
package.json
node_modules/

# IndexNow key reference note (the public key file itself IS committed)
.indexnow-key.txt
